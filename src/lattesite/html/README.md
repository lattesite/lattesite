# LatteSite - HTML

This subpackage provides:

- Typed Java classes for common HTML elements (e.g. `div`, `span`, `a`, `img`, `table`, etc.)
- HTML attribute and inline style management
- ARIA role support
- HTML document tree rendering via `ElementGenerator`
