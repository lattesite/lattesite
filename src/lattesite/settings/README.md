# LatteSite - Settings

This subpackage provides:

- Site-wide configuration via `SiteSettings`
- Locale list management
- Configurable output paths for the public folder, static root assets, and static locale assets
