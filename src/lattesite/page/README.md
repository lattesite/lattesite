# LatteSite - Page

This subpackage provides:

- The `Page` interface contract that all site pages must implement
- Per-locale path and title resolution
- Hooks for injecting head and body elements into the rendered HTML document
