# LatteSite - Localization

This subpackage provides:

- Locale definitions with locale code, base URL, and output subfolder name
- Strongly-typed localized text classes for each supported language (e.g. `English`, `Swedish`, `German`, `French`, etc.)
- Building blocks for implementing translation utilities in your site
