# LatteSite - Structured Data

This subpackage provides:

- Schema.org structured data models
- JSON-LD generation utilities
- Support for common structured data types (e.g. Product, Article, Organization)
- Integration with LatteSite page and content generation
